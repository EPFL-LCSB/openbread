sudo: required

services:
  - docker

language: python
python:
  - "3.5"
branches:
 only:
 #- master
 #- dev
 - ci

before_install:
 - pip install pytest-cov
 - docker pull danielweilandt/openbread_docker

script:
# - bash ./run_tests.sh
 - bash -c "echo hello"

after_success:
- pip install codecov; codecov;